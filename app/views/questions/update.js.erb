$('.question .question-error-<%= @question.id %>').html('<%= render 'shared/errors', resource: @question %>');

<% unless @question.errors.present? %>
  $('.question').html("<%= j render 'question_main' %>")
<% end %>
